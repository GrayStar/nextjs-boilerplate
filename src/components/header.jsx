import { Container, Row, Col } from 'react-bootstrap';

const Header = () => {
    return (
        <header>
            <Container>
                <Row>
                    <Col>
                        Header
                    </Col>
                </Row>
            </Container>
        </header>
    );
};

export default Header;